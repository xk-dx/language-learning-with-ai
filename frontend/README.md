# WordForge 前端

这是一个独立的 React + Vite 前端原型，只保留最核心的词表、学习、测验和进度界面，打开就能用，不依赖后端。

## 启动

```bash
npm install
npm run dev
```

## 说明

- 数据先存储在浏览器本地 `localStorage`
- 支持创建词表、添加单词、删除词表/单词
- 支持学习模式和测验模式
- 默认界面为中文
